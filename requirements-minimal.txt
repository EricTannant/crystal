# Crystal - Minimal Working Requirements
# This version uses Pydantic v1 to avoid Rust compilation issues

# Core Web Framework
fastapi==0.103.0
uvicorn==0.23.0

# Configuration & Settings (v1 to avoid compilation)
pydantic==1.10.12

# Database (SQLite support built-in, PostgreSQL optional)
sqlalchemy==2.0.20

# CLI Framework (without extras to avoid compilation issues)
typer==0.9.0
rich==13.6.0

# HTTP Client
requests==2.31.0

# Task Scheduling
apscheduler==3.10.1

# Logging (simple versions)
structlog==23.1.0

# Optional: Add these later if needed
openai==1.3.0
psutil==5.9.0
alembic==1.12.0
